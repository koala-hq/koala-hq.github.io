{
  "name": "brochure-site",
  "private": true,
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./main.css -o ./dist/output.css",
    "build:watch": "npx @tailwindcss/cli -i ./main.css -o ./dist/output.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "tailwindcss": "^4"
  }
}
